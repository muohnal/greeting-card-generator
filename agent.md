# 인사카드 이미지 생성기

보험 설계사가 고객에게 보낼 인사 카드를 이름만 바꿔가며 대량으로 뽑는 도구.

## 핵심 요구사항

- 인터넷 없이도 열리는 단일 `.html` 파일 (외부 CDN 최소화, 실패 시 폴백)
- 고객 최대 500명까지 처리
- 이름 목록은 (1) 줄바꿈/쉼표 붙여넣기 (2) CSV 업로드 두 가지 방식
- 결과물은 PNG를 ZIP 한 파일로 묶어 다운로드. 파일명 `001_김원정.png` 형식
- 개별 다운로드 500번은 브라우저가 차단하므로 반드시 ZIP

## 성능

- html2canvas는 500장에 너무 느림 → Canvas 2D API로 직접 그림
- 배치 처리 + 진행률 표시 (n/500, 예상 남은 시간)
- 중간에 멈춤/재개 가능
- 메모리 터지지 않게 청크 단위 처리

## 카드 편집

- 상단 회사 로고는 **기본으로 포함**되어 있고, 업로드로 교체 가능 (크기·위치 조절)
- 텍스트: 인사말, 이름, 직함, 소속, 서비스 3개, 전화번호, 하단 안내문
- 한글 웹폰트 선택. 로드 실패한 폰트는 '사용 불가' 표시
- 이름 글꼴/크기 조절, 직함은 이름의 2/3 크기로 자동 연동
- 설정은 localStorage 저장

## 안전장치

- 생성 전 미리보기 3장 샘플 확인
- 이름 목록에 중복·빈칸·이상한 문자 있으면 경고
- 발송 담당자 정보가 기본값 그대로면 경고

## 디자인

- 출력 해상도 가로 1080px
- 남색 `#102D4A`, 금색 `#C6A05C`, 배경 `#FAF9F6`
- 모바일에서도 미리보기가 비율대로 축소

---

## 구현하며 내린 결정

| 항목 | 결정 | 이유 |
|---|---|---|
| ZIP | **직접 구현** (STORE 방식, ~90줄) | PNG는 이미 압축돼 있어 deflate가 1~2%밖에 못 줄임. 압축을 안 쓰니 JSZip이 필요 없고, 외부 의존성 0이 되어 오프라인 요구사항을 진짜로 만족 |
| ZIP 분할 | 200장마다 | 500장 = 150~300MB. 한 덩어리로 만들면 모바일 크롬이 죽음 |
| 웹폰트 | CDN 유지 + 시스템 고딕 폴백 | 한글 폰트 6종 인라인 시 파일이 20MB. 폰트만 CDN이고 나머지 기능은 전부 오프라인 동작 |
| 편집 방식 | contenteditable → 좌측 폼 입력 | Canvas는 픽셀이라 글자 클릭 편집이 불가능. 대신 500장 생성이 가능해짐 |
| 배경 캐시 | 이름 뺀 카드를 1회만 그려 재사용 | 500장 중 바뀌는 건 인사말 한 줄뿐 |
| 명단 저장 | 저장 안 함 | 고객명은 개인정보 |
| CSV 인코딩 | UTF-8 실패 시 EUC-KR 재시도 | 한국 엑셀 CSV는 대부분 CP949 |
| 기본 로고 | base64로 파일 안에 내장 (108KB) | 인터넷이 끊겨도 로고가 그대로 나와야 함. 흰 여백을 잘라내고 배경을 투명 처리한 480px PNG |

## 나중에 추가한 기능

### 글꼴 4종 내장 (2026-08-06)

아티팩트 호스팅이 CSP로 외부 폰트를 전부 막아서 폰에서는 글꼴을 하나도 고를 수 없었다.
프리텐다드·마루부리·검은고딕·주아를 **완성형 2,350자 + 영숫자 + 기호로 잘라내(fontTools)**
base64 로 파일에 심었다. 원본 5.4MB → 1.5MB, 최종 HTML 165KB → 2.2MB.
이제 인터넷이 없어도, 외부 폰트를 막는 환경에서도 ★ 4종은 항상 쓸 수 있다.

### 색 테마 (2026-08-06)

배경·메인·포인트 3색을 따로 고르고, 프리셋 6종(아이보리·화이트·네이버 녹색·회색·딥블루·버건디)으로
한 번에 바꿀 수 있다. **메인 색의 밝기를 계산해 밴드 위 글자색을 자동으로 뒤집는다** —
네이버 녹색처럼 밝은 색에서 흰 글자가 안 보이는 문제를 막기 위해서다.
인사말 본문·연락처 라벨 같은 연한 보조색도 메인 색에서 계산한다.

### 공유 (2026-08-07)

카카오 SDK 대신 **OS 공유 시트(`navigator.share`)** 를 쓴다.

| | 카카오 링크 SDK | Web Share API |
|---|---|---|
| 준비물 | 개발자 앱 등록 + 자체 도메인 | 없음 |
| 로컬 이미지 | **불가** (카카오 서버 업로드 필요) | 파일 그대로 전달 |
| 도달 앱 | 카톡만 | 카톡·문자·인스타·드라이브 전부 |

카드 1장 공유 / 이미지 20장 이하 일괄 공유 / ZIP 공유 세 가지. 실패 상황 4가지
(미지원·파일공유 차단·iframe 권한 차단·사용자 취소)를 각각 다르게 안내한다.

`waitFont()` 에 4초 상한을 뒀다. 시작 직후에는 CDN 글꼴 확인이 아직 돌고 있어서
공유 버튼이 그 뒤로 밀릴 수 있기 때문이다. 실측 응답은 평균 58ms.

## 검증 중에 잡은 두 가지 실제 버그

### 1. 글꼴 "사용 불가" 표시가 동작하지 않았다

v9에서 가져온 `document.fonts.check()` 방식은 **CDN이 완전히 차단된 상태에서도 모든 글꼴을
"사용 가능"이라고 보고**했다. 해당 `@font-face` 규칙이 아예 없으면 "시스템 폰트로 그릴 수
있다"는 뜻에서 `true`를 돌려주기 때문이다.

→ 기본 글꼴(monospace/serif/sans-serif)로 잰 글자 폭과 비교해서 **실제로 다른 글꼴이
적용됐는지 측정**하는 방식으로 교체했다. `resolveFonts()`가 시작할 때 한 번만 돌고
결과를 `FONT_OK`에 캐시한다.

### 2. 글꼴 CDN 주소 6개 중 4개가 404였다

위 탐지를 고치고 나서야 드러난 문제. v9의 주소를 그대로 썼는데 실제로는:

| 주소 | 상태 |
|---|---|
| `webfontworld/score/SCoreDream.css` | **404** (저장소 자체가 사라짐) |
| `webfontworld/naver/MaruBuri.css` | **404** |
| `noonfonts_2001@1.1/BMEULJIRO.css` | **404** |
| `noonfonts_2001@1.1/Cafe24Oneprettynight.css` | **404** |
| `sun-typeface/SUIT/.../SUIT.css` | **404** (버전 태그 필요) |

마루부리를 골랐을 때 명조체가 나오길래 잘 되는 줄 알았는데, 실은 폴백인 나눔명조가
그려지고 있었다. 살아있는 주소로 전부 교체:

- SUIT → `sun-typeface/SUIT@2.0.5/fonts/variable/woff2/SUIT-Variable.css`
- 마루부리 → 네이버 공식 `hangeul.pstatic.net/hangeul_static/css/maru-buri.css`
- 에스코어드림·을지로체·고운밤 → 구글 폰트로 대체 (IBM Plex Sans KR, 검은고딕, 주아,
  고운돋움, 고운바탕). 구글 폰트는 죽을 일이 없다.

## 성능 한계

`canvas.toBlob()` PNG 인코딩이 1080×~1900 한 장에 40~80ms. 500장이면 **20~40초**가 하한선이고
이건 어떤 방법으로도 줄지 않는다. 그래서 UI를 "빠르게"가 아니라 "정직한 진행률"로 설계했다.
예상 시간은 최근 10장 실측 평균으로 계산한다.
